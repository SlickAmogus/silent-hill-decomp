/* Floatstinger acid-projectile support (sharedFunc_800CB0A4_4_s03) — only
 * map4_s03/map4_s05 DLLs compile it natively, absent from the exe import
 * lib, so the global chara DLL carries its own copy. */
#include "../src/maps/particle_acid.c"
